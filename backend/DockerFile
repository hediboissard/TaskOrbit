FROM node:18-alpine

WORKDIR /app

# Le build context peut être la racine du monorepo.
# On copie donc explicitement les fichiers du backend.
COPY backend/package.json backend/package-lock.json ./
RUN npm ci --omit=dev

COPY backend/ ./

EXPOSE 5001

CMD ["npm", "start"]
